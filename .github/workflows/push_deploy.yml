name: Update Docker and Deploy (Production)

on:
  workflow_dispatch:
  push:
    branches: [ master ]

jobs:
  update-docker:
    uses: RDGAapp/web/.github/workflows/push.yml@master
    secrets:
      DOCKER_LOGIN: ${{ secrets.DOCKER_LOGIN }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}